/** Lifecycle stage ids used across the customer workspace shell. */
export type Stage =
  | "customer"
  | "tasks"
  | "threads"
  | "quote"
  | "contract"
  | "invoicing"
  | "payment"
  | "revrec";
